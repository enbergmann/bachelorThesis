% used packages

\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{subcaption}

% --- REFERENCING I-------------------------------------------------------------

\usepackage[hidelinks]{hyperref} % has to be loaded before ams to avoid warnings

\usepackage[backend=biber, style = alphabetic]{biblatex}
\usepackage{csquotes} % to fix biblatex warning expecting csquotes
\addbibresource{sourcesBergmannBT.bib}

%\emergencystretch=1em % allows for an additional line-breaking pass with the 
                      % amount of tolerable white space per line increased
                      % by 1em
                      % fixes overfull hbox in bibliography
                      %TODO check this and other options, maybe see if this 
                      % can be done only local, check \sloppy and \fussy

% --- MATH ---------------------------------------------------------------------

\usepackage{amsmath} % align
\usepackage{amsthm} % proof
\usepackage{amssymb}
\usepackage{mathtools} %coloneqq
\usepackage{mathabx} %vvvert


% --- REFERENCING II -----------------------------------------------------------

\usepackage[\lang]{cleveref} % has to be loaded after ams and hyperref

% --- MISCELLANEOUS ------------------------------------------------------------

\usepackage{ifthen}
